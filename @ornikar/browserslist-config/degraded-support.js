'use strict';

// Note: if you update this, update the link in README.md !

module.exports =
  'last 4 versions, > .2% in FR, Firefox ESR, not ie < 10, not ios < 10, not op_mini all, not dead'.split(',');
